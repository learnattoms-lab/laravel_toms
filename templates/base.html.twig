<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ page_title|default('Toms Music School') }}{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Custom CSS -->
    <style>
        :root {
            --primary-color: #3b82f6;
            --secondary-color: #6366f1;
            --accent-color: #f59e0b;
            --text-dark: #1f2937;
            --text-light: #6b7280;
            --bg-light: #f9fafb;
            --bg-white: #ffffff;
            --border-color: #e5e7eb;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            background-color: var(--bg-white);
        }
        
        .navbar {
            background: linear-gradient(135deg, #3b82f6, #6366f1);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: white !important;
        }
        
        .nav-link {
            color: rgba(255, 255, 255, 0.9) !important;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .nav-link:hover {
            color: white !important;
            transform: translateY(-2px);
        }
        
        .hero-section {
            background: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%);
            min-height: 80vh;
            display: flex;
            align-items: center;
        }
        
        /* Image styling for consistent appearance */
        .instrument-image {
            height: 250px;
            object-fit: cover;
            object-position: center;
            width: 100%;
        }
        
        .hero-section img {
            max-height: 500px;
            object-fit: cover;
            object-position: center;
        }
        
        .performance-gallery img {
            height: 350px;
            object-fit: cover;
            object-position: center;
            width: 100%;
            display: block;
        }
        
        .performance-card {
            position: relative;
            overflow: hidden;
            border-radius: 0.75rem;
            transition: transform 0.3s ease;
            height: 350px;
        }
        
        .performance-card:hover {
            transform: translateY(-5px);
        }
        
        .performance-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
            color: white;
            padding: 2rem 1.5rem 1.5rem;
            transform: translateY(100%);
            transition: transform 0.3s ease;
        }
        
        .performance-card:hover .performance-overlay {
            transform: translateY(0);
        }
        
        .performance-overlay h5 {
            margin-bottom: 0.5rem;
            font-size: 1.25rem;
        }
        
        .performance-overlay p {
            margin-bottom: 0;
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        /* Fix alignment issues */
        .performance-card img {
            vertical-align: top;
            margin: 0;
            padding: 0;
        }
        
        .row.g-4 {
            align-items: stretch;
        }
        
        .col-lg-5, .col-md-6 {
            display: flex;
            flex-direction: column;
        }
        
        /* Enhanced section styling */
        .bg-light {
            background-color: #f8f9fa !important;
        }
        
        .text-dark {
            color: #212529 !important;
        }
        
        .fs-5 {
            font-size: 1.25rem !important;
        }
        
        /* Override Bootstrap colors with balanced palette */
        .bg-primary {
            background-color: #3b82f6 !important;
        }
        
        .bg-secondary {
            background-color: #6366f1 !important;
        }
        
        .btn-primary {
            background-color: #3b82f6 !important;
            border-color: #3b82f6 !important;
        }
        
        .btn-primary:hover {
            background-color: #2563eb !important;
            border-color: #2563eb !important;
        }
        
        .btn-outline-light:hover {
            background-color: #6366f1 !important;
            border-color: #6366f1 !important;
        }

        .step-number {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: bold;
            margin: 0 auto;
        }

        .feature-icon {
            color: #3b82f6;
        }

        .stars {
            color: #ffc107;
        }

        .card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 100%;
        }
        
        .card-img-top {
            border-top-left-radius: calc(0.375rem - 1px);
            border-top-right-radius: calc(0.375rem - 1px);
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1) !important;
        }

        .hero-section img {
            transition: transform 0.3s ease;
        }

        .hero-section img:hover {
            transform: scale(1.05);
        }
        
        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="50" cy="50" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            opacity: 0.3;
        }
        
        .hero-content {
            position: relative;
            z-index: 2;
        }
        
        .hero-title {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }
        
        .hero-subtitle {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        
        .btn-primary-custom {
            background: var(--accent-color);
            border: none;
            padding: 15px 30px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(245, 158, 11, 0.3);
        }
        
        .btn-primary-custom:hover {
            background: #d97706;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(245, 158, 11, 0.4);
        }
        
        .btn-secondary-custom {
            background: transparent;
            border: 2px solid white;
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        
        .btn-secondary-custom:hover {
            background: white;
            color: var(--primary-color);
            transform: translateY(-2px);
        }
        
        .section {
            padding: 80px 0;
        }
        
        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 3rem;
            color: var(--text-dark);
        }
        
        .feature-card {
            background: var(--bg-white);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            height: 100%;
            border: 1px solid var(--border-color);
        }
        
        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }
        
        .feature-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            color: white;
            font-size: 2rem;
        }
        
        .feature-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--text-dark);
        }
        
        .feature-description {
            color: var(--text-light);
            line-height: 1.6;
        }
        
        .testimonial-card {
            background: var(--bg-white);
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border: 1px solid var(--border-color);
        }
        
        .testimonial-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        
        .footer {
            background: var(--text-dark);
            color: white;
            padding: 60px 0 30px;
        }
        
        .footer-title {
            color: white;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
        }
        
        .footer-link {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: color 0.3s ease;
            display: block;
            margin-bottom: 0.5rem;
        }
        
        .footer-link:hover {
            color: white;
            text-decoration: none;
        }
        
        .footer p {
            color: rgba(255, 255, 255, 0.8);
        }
        
        .footer ul li {
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 0.5rem;
        }
        
        .footer .text-muted {
            color: rgba(255, 255, 255, 0.7) !important;
        }
        
        .footer hr {
            border-color: rgba(255, 255, 255, 0.2);
        }
        
        .footer .fa-heart {
            color: #ef4444 !important;
        }
        
        .footer a {
            color: rgba(255, 255, 255, 0.8);
        }
        
        .footer a:hover {
            color: white;
        }
        
        .footer .fas {
            color: rgba(255, 255, 255, 0.8);
            width: 20px;
        }
        
        .footer .contact-info li {
            display: flex;
            align-items: center;
            margin-bottom: 0.75rem;
        }
        
        .social-icon {
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-right: 10px;
            transition: all 0.3s ease;
            color: white;
            text-decoration: none;
        }
        
        .social-icon:hover {
            background: var(--accent-color);
            transform: translateY(-2px);
            color: white;
        }
        
        .social-icon i {
            color: white;
            font-size: 1.2rem;
        }
        
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .hero-section {
                padding: 80px 0 60px;
            }
            
            .section {
                padding: 60px 0;
            }
        }
        
        /* Custom Animated Icons */
        .custom-icon {
            position: relative;
            display: inline-block;
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 50%;
            margin: 0 auto 1.5rem;
            transition: all 0.3s ease;
            overflow: hidden;
        }
        
        .custom-icon::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-family: 'Font Awesome 6 Free';
            font-weight: 900;
            font-size: 2rem;
            color: white;
            z-index: 2;
        }
        
        .custom-icon::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s ease;
        }
        
        .custom-icon:hover::after {
            left: 100%;
        }
        
        .custom-icon:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 10px 25px rgba(59, 130, 246, 0.3);
        }
        
        /* Specific icon styles */
        .icon-graduate::before {
            content: '\f51a'; /* fa-user-graduate */
        }
        
        .icon-clock::before {
            content: '\f017'; /* fa-clock */
        }
        
        .icon-chart::before {
            content: '\f201'; /* fa-chart-line */
        }
        
        .icon-music::before {
            content: '\f001'; /* fa-music */
        }
        
        .icon-heart::before {
            content: '\f004'; /* fa-heart */
        }
        
        .icon-star::before {
            content: '\f005'; /* fa-star */
        }
        
        /* Icon animations */
        .custom-icon.animate-pulse {
            animation: pulse 2s infinite;
        }
        
        .custom-icon.animate-bounce {
            animation: bounce 2s infinite;
        }
        
        .custom-icon.animate-rotate {
            animation: rotate 3s linear infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
        
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }
        
        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        /* Carousel Styles */
        .carousel-section {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%) !important;
            padding: 4rem 0;
        }
        
        .carousel-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: white;
        }
        
        .carousel-text {
            font-size: 1.1rem;
            margin-bottom: 2rem;
            color: rgba(255, 255, 255, 0.9);
            line-height: 1.6;
        }
        
        .carousel-buttons {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }
        
        .carousel-buttons .btn {
            transition: all 0.3s ease;
        }
        
        .carousel-buttons .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 0.25rem 0.5rem rgba(0, 0, 0, 0.2);
        }
        
        .carousel-item {
            padding: 2rem 0;
        }
        
        .carousel-item img {
            border-radius: 1rem;
            box-shadow: 0 1rem 3rem rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }
        
        .carousel-item img:hover {
            transform: scale(1.02);
        }
        
        .carousel-indicators {
            bottom: -3rem;
        }
        
        .carousel-indicators button {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.5);
            border: none;
            margin: 0 5px;
        }
        
        .carousel-indicators button.active {
            background-color: white;
        }
        
        .carousel-control-prev,
        .carousel-control-next {
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            border: none;
            top: 50%;
            transform: translateY(-50%);
        }
        
        .carousel-control-prev {
            left: 2rem;
        }
        
        .carousel-control-next {
            right: 2rem;
        }
        
        .carousel-control-prev:hover,
        .carousel-control-next:hover {
            background: rgba(255, 255, 255, 0.3);
        }
        
        @media (max-width: 768px) {
            .carousel-title {
                font-size: 2rem;
            }
            
            .carousel-text {
                font-size: 1rem;
            }
            
            .carousel-control-prev,
            .carousel-control-next {
                width: 40px;
                height: 40px;
            }
            
            .carousel-control-prev {
                left: 1rem;
            }
            
            .carousel-control-next {
                right: 1rem;
            }
        }
        
        /* Floating Request Call Button */
        .floating-request-call {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            z-index: 1000;
            opacity: 0;
            transform: translateY(100px);
            transition: all 0.5s ease;
        }
        
        .floating-request-call.show {
            opacity: 1;
            transform: translateY(0);
        }
        
        .request-call-btn {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border: none;
            border-radius: 50px;
            padding: 1rem 1.5rem;
            font-weight: 600;
            font-size: 1rem;
            box-shadow: 0 0.5rem 1.5rem rgba(40, 167, 69, 0.3);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
        }
        
        .request-call-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 0.75rem 2rem rgba(40, 167, 69, 0.4);
            color: white;
            text-decoration: none;
        }
        
        .request-call-btn i {
            font-size: 1.2rem;
        }
        
        .request-call-modal {
            display: none;
            position: fixed;
            z-index: 1001;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
        }
        
        .request-call-content {
            background-color: white;
            margin: 5% auto;
            padding: 2rem;
            border-radius: 1rem;
            width: 90%;
            max-width: 500px;
            position: relative;
            animation: modalSlideIn 0.3s ease;
        }
        
        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #e9ecef;
        }
        
        .modal-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #212529;
            margin: 0;
        }
        
        .close-modal {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: #6c757d;
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 0.25rem;
            transition: all 0.3s ease;
        }
        
        .close-modal:hover {
            background: #f8f9fa;
            color: #495057;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-label {
            font-weight: 500;
            color: #495057;
            margin-bottom: 0.5rem;
            display: block;
        }
        
        .form-control {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #e9ecef;
            border-radius: 0.25rem;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }
        
        .form-control:focus {
            outline: none;
            border-color: #28a745;
            box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
        }
        
        .submit-request {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border: none;
            border-radius: 0.25rem;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }
        
        .submit-request:hover {
            transform: translateY(-1px);
            box-shadow: 0 0.25rem 0.5rem rgba(40, 167, 69, 0.3);
        }
        
        @media (max-width: 768px) {
            .floating-request-call {
                bottom: 1rem;
                right: 1rem;
            }
            
            .request-call-btn {
                padding: 0.75rem 1.25rem;
                font-size: 0.875rem;
            }
            
            .request-call-content {
                margin: 10% auto;
                width: 95%;
                padding: 1.5rem;
            }
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <i class="fas fa-music me-2"></i>Toms Music School
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_about') }}">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('teachers') }}">Teachers</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('plans') }}">Plans</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                </ul>
                
                <ul class="navbar-nav">
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-user me-2"></i>{{ app.user.firstName|default(app.user.email) }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                                {% if 'ROLE_ADMIN' in app.user.roles %}
                                    <li><a class="dropdown-item" href="{{ path('admin_dashboard') }}"><i class="fas fa-shield-alt me-2"></i>Admin Dashboard</a></li>
                                    <li><a class="dropdown-item" href="{{ path('admin_teachers') }}"><i class="fas fa-chalkboard-teacher me-2"></i>Manage Teachers</a></li>
                                    <li><a class="dropdown-item" href="{{ path('admin_students') }}"><i class="fas fa-users me-2"></i>Manage Students</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                {% elseif 'ROLE_TEACHER' in app.user.roles %}
                                    <li><a class="dropdown-item" href="{{ path('user_dashboard') }}"><i class="fas fa-tachometer-alt me-2"></i>Teacher Dashboard</a></li>
                                    <li><a class="dropdown-item" href="{{ path('user_profile') }}"><i class="fas fa-user-edit me-2"></i>My Profile</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                {% else %}
                                    <li><a class="dropdown-item" href="{{ path('user_dashboard') }}"><i class="fas fa-tachometer-alt me-2"></i>Dashboard</a></li>
                                    <li><a class="dropdown-item" href="{{ path('user_profile') }}"><i class="fas fa-user-edit me-2"></i>Profile</a></li>
                                    <li><a class="dropdown-item" href="{{ path('user_progress') }}"><i class="fas fa-chart-line me-2"></i>Progress</a></li>
                                    <li><a class="dropdown-item" href="{{ path('user_achievements') }}"><i class="fas fa-trophy me-2"></i>Achievements</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                {% endif %}
                                <li><a class="dropdown-item" href="{{ path('user_settings') }}"><i class="fas fa-cog me-2"></i>Settings</a></li>
                                <li><a class="dropdown-item" href="{{ path('logout') }}"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="loginDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-sign-in-alt me-2"></i>Login
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="loginDropdown">
                                <li><a class="dropdown-item" href="{{ path('login') }}"><i class="fas fa-user-graduate me-2"></i>Student Login</a></li>
                                <li><a class="dropdown-item" href="{{ path('teacher_login') }}"><i class="fas fa-chalkboard-teacher me-2"></i>Teacher Login</a></li>
                            </ul>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('register') }}">Register</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-md-6 mb-4">
                    <h5 class="footer-title">Toms Music School</h5>
                    <p class="text-muted">Empowering students to discover their musical potential through personalized online lessons with expert teachers.</p>
                    <div class="social-icons">
                        <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="social-icon"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="social-icon"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                
                <div class="col-lg-2 col-md-6 mb-4">
                    <h5 class="footer-title">Quick Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_home') }}" class="footer-link">Home</a></li>
                        <li><a href="{{ path('app_about') }}" class="footer-link">About</a></li>
                        <li><a href="{{ path('teachers') }}" class="footer-link">Teachers</a></li>
                        <li><a href="{{ path('plans') }}" class="footer-link">Plans</a></li>
                    </ul>
                </div>
                
                <div class="col-lg-3 col-md-6 mb-4">
                    <h5 class="footer-title">Support</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_contact') }}" class="footer-link">Contact Us</a></li>
                        <li><a href="#" class="footer-link">Help Center</a></li>
                        <li><a href="#" class="footer-link">Privacy Policy</a></li>
                        <li><a href="#" class="footer-link">Terms of Service</a></li>
                    </ul>
                </div>
                
                <div class="col-lg-3 col-md-6 mb-4">
                    <h5 class="footer-title">Contact Info</h5>
                    <ul class="list-unstyled text-muted contact-info">
                        <li><i class="fas fa-envelope me-2"></i>info@tomsschool.com</li>
                        <li><i class="fas fa-phone me-2"></i>+1 (555) 123-4567</li>
                        <li><i class="fas fa-map-marker-alt me-2"></i>123 Music Street, NY 10001</li>
                    </ul>
                </div>
            </div>
            
            <hr class="my-4" style="border-color: rgba(255,255,255,0.1);">
            
            <div class="row align-items-center">
                <div class="col-md-6">
                    <p class="text-muted mb-0">&copy; 2024 Toms Music School. All rights reserved.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="text-muted mb-0">Made with <i class="fas fa-heart text-danger"></i> for music lovers</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JS -->
    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Navbar background change on scroll
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(59, 130, 246, 0.95)';
            } else {
                navbar.style.background = 'linear-gradient(135deg, #3b82f6, #6366f1)';
            }
        });
    </script>
    
    {% block javascripts %}{% endblock %}
    
    <!-- Floating Request Call Button -->
    <div class="floating-request-call" id="floatingRequestCall">
        <button class="request-call-btn" onclick="openRequestCallModal()">
            <i class="fas fa-phone"></i>
            Request Call
        </button>
    </div>

    <!-- Request Call Modal -->
    <div class="request-call-modal" id="requestCallModal">
        <div class="request-call-content">
            <div class="modal-header">
                <h3 class="modal-title">Request a Call Back</h3>
                <button class="close-modal" onclick="closeRequestCallModal()">&times;</button>
            </div>
            <form id="requestCallForm">
                <div class="form-group">
                    <label class="form-label" for="callName">Full Name *</label>
                    <input type="text" class="form-control" id="callName" name="name" required>
                </div>
                <div class="form-group">
                    <label class="form-label" for="callPhone">Phone Number *</label>
                    <input type="tel" class="form-control" id="callPhone" name="phone" required>
                </div>
                <div class="form-group">
                    <label class="form-label" for="callEmail">Email Address</label>
                    <input type="email" class="form-control" id="callEmail" name="email">
                </div>
                <div class="form-group">
                    <label class="form-label" for="callMessage">Message</label>
                    <textarea class="form-control" id="callMessage" name="message" rows="3" placeholder="Tell us about your musical interests and what you'd like to learn..."></textarea>
                </div>
                <button type="submit" class="submit-request">
                    <i class="fas fa-phone me-2"></i>Request Call Back
                </button>
            </form>
        </div>
    </div>

    <script>
        // Floating request call button functionality
        let requestCallShown = false;
        
        window.addEventListener('scroll', function() {
            const scrollY = window.scrollY;
            const floatingButton = document.getElementById('floatingRequestCall');
            
            // Show button after scrolling down 300px
            if (scrollY > 300 && !requestCallShown) {
                floatingButton.classList.add('show');
                requestCallShown = true;
            } else if (scrollY <= 300 && requestCallShown) {
                floatingButton.classList.remove('show');
                requestCallShown = false;
            }
        });
        
        function openRequestCallModal() {
            document.getElementById('requestCallModal').style.display = 'block';
            document.body.style.overflow = 'hidden';
        }
        
        function closeRequestCallModal() {
            document.getElementById('requestCallModal').style.display = 'none';
            document.body.style.overflow = 'auto';
        }
        
        // Close modal when clicking outside
        window.addEventListener('click', function(event) {
            const modal = document.getElementById('requestCallModal');
            if (event.target === modal) {
                closeRequestCallModal();
            }
        });
        
        // Handle form submission
        document.getElementById('requestCallForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Get form data
            const formData = new FormData(this);
            const data = Object.fromEntries(formData);
            
            // Here you would typically send the data to your backend
            console.log('Request call data:', data);
            
            // Show success message
            alert('Thank you! We\'ll call you back within 24 hours.');
            
            // Close modal and reset form
            closeRequestCallModal();
            this.reset();
        });
    </script>
</body>
</html>
